<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>
  <ItemGroup>
    <!-- The exact StockSharp indicators the terminal uses (source, not the lagging NuGet). If the
         sibling StockSharp checkout is absent the build fails and the parity test skips. -->
    <ProjectReference Include="..\..\..\StockSharp (GitHub)\Algo.Indicators\Algo.Indicators.csproj" />
  </ItemGroup>
</Project>
